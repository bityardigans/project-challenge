<%= simple_form_for @dog, :html=> { class: 'ui form' }  do |f| %>
  <%= f.input :name %>
  <%= f.input :description, as: :text %>
  <%= f.input :image, as: :file, :class => 'ui button' %>
  
  <%= f.button :submit, :class => 'ui button' %>
<% end %>
